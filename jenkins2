pipeline {
  agent any
  stages {
    stage("check"){
      steps{
        script{
          env.URL = sh(script: "echo ${env.GIT_URL} | cut -d'/' -f 3-5", returnStdout: true)
          sh "echo ${env.URL}"
          
      
        }
      }
    }
  }
}
