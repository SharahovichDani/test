pipeline {
  agent any
  stages {
    stage("check"){
      steps{
        script{
          env.URL = sh(script: "echo ${env.GIT_URL} | cut -d'/' -f 4-5", returnStdout: true)
          sh "echo ${env.URL}"
          
      
        }
      }
    }
  }
}
