pipeline {
  agent any
  stages {
    stage("check"){
      steps{
        script{
          env.URL = sh(script: '''echo '''+env.GIT_URL+''' | awk  -F'/' '{print $2}'''', returnStdout: true)
          sh "echo ${env.URL}"
          
      
        }
      }
    }
  }
}
