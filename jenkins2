pipeline {
  agent any
  stages {
    stage("check"){
      steps{
        script{
          env.URL = sh(script: "echo ${env.GIT_URL} | awk '{print $1}'", returnStdout: true)
          sh "echo ${env.URL}"
          
      
        }
      }
    }
  }
}
