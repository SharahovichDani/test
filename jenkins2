pipeline {
  agent any
  stages {
    stage("check"){
      steps{
        script{
          env.URL = sh(script: "echo ${env.GIT_URL} | cut -d'/' -f 2-3", returnStdout: true)
          sh "echo ${env.URL}"
          
      
        }
      }
    }
  }
}
