pipeline {
  agent any
  stages {
    stage("check"){
      steps{
        script{
          sh '''
          echo "${env.GET_URL} | awk  -F'/' '{print $2}'"
          '''
          
      
        }
      }
    }
  }
}
