pipeline {
  agent any
  stages {
    stage("check"){
      steps{
        script{
          env.URL = sh(script: "echo ${GIT_URL}", returnStdout: true)
          sh "echo ${env.URL}"
          
      
        }
      }
    }
  }
}
